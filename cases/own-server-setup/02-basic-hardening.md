← [План](plan.md)

# Этап 2. Базовая защита сервера

**Статус:** ✅ завершено

**Задача:** закрыть очевидные дыры до того, как сервер увидят боты-сканеры (а они найдут его в течение часов после включения).

## Пошагово
- [x] Обновить систему: `apt update && apt upgrade -y`
- [x] Создать обычного пользователя с правами sudo (не работать постоянно под root):
  `adduser ваше_имя` → `usermod -aG sudo ваше_имя`
- [x] Настроить вход по SSH-ключу вместо пароля (для одного администратора):
  - Сгенерировать ключ на своём компьютере (`ssh-keygen`)
  - Скопировать публичный ключ на сервер (`ssh-copy-id`)
- [ ] ~~Настроить SSH-ключ для второго администратора~~ — второго администратора нет, пункт снят
- [x] Отключить вход по паролю и вход под root:
  - `PasswordAuthentication no`
  - `PermitRootLogin no`
- [x] Сменить порт SSH с 22 на нестандартный (снижает шум от автоматических атак)
- [x] Установить `fail2ban` — блокирует IP после нескольких неудачных попыток входа
- [x] Настроить firewall (`ufw`):
  - Разрешить только нужные порты (новый SSH-порт, позже — VPN, SIP)
  - Всё остальное — закрыть

## Что нужно узнать
- [x] Что такое SSH-ключи (публичный/приватный) и почему это безопаснее пароля
- [x] Как не потерять доступ к серверу, если ошибётесь в конфиге SSH (держали рабочую сессию открытой и проверяли новые настройки в отдельном окне, прежде чем закрыть старую)
- [x] Базовые команды `ufw`: allow, deny, status

## Что сделано на практике
- Создан непривилегированный пользователь с правами `sudo` (отдельного второго администратора решили не заводить)
- Настроен вход по SSH-ключу (ed25519) вместо пароля — ключ сгенерирован на клиенте, публичная часть добавлена в `~/.ssh/authorized_keys` на сервере
- Обнаружено, что хостинг-провайдер по умолчанию уже включил `ufw` с базовыми правилами (80, 443, 22 + порт от предустановленной панели управления, которую не используем). SSH (22) и 80/443 оставлены открытыми (планируется хостинг сайтов), порт неиспользуемой панели оставлен как есть — можно закрыть в любой момент командой `ufw delete <номер>`
- Установлен и настроен `fail2ban` для защиты SSH от подбора пароля: 5 неудачных попыток за 10 минут → блокировка IP на 1 час. Конфигурация вынесена в отдельный файл `/etc/fail2ban/jail.local`, не трогая основной конфиг пакета — чтобы настройки не слетели при обновлении
- Отключены `PasswordAuthentication` и `PermitRootLogin`, проверено через `sudo sshd -T` (эффективная конфигурация) и практическим тестом: вход обычным пользователем по ключу проходит без пароля, `ssh root@ip` получает `Permission denied`
- Порт SSH сменён с 22 на 2244: открыт новый порт в `ufw` (`ufw allow 2244/tcp`), обновлён `sshd_config`, а также юнит `ssh.socket` (см. ниже), после проверки удалено правило 22/tcp (IPv4 и IPv6) из `ufw`
- Настроен доступ к серверу с мобильных устройств (телефон, планшет) через приложение Termius: на каждом устройстве сгенерирован **отдельный** SSH-ключ (ed25519) во встроенном Keychain приложения — приватный ключ никогда не покидает устройство, на сервер добавлена только публичная часть (отдельной строкой в `~/.ssh/authorized_keys`, без удаления существующих ключей). В настройках подключения в Termius указан нестандартный порт 2244

## Результат
Базовая защита полностью внедрена: sudo-пользователь, вход по SSH-ключу, firewall, fail2ban, отключён вход по паролю и под root, SSH перенесён с 22 на 2244. Подтверждено практически: подключение по 2244 работает, по 22 — `Connection timed out`.

## Проблемы и решения
- В Windows PowerShell нет команды `ssh-copy-id` (это Linux/macOS утилита) — ключ добавляли вручную через `echo "публичный_ключ" >> ~/.ssh/authorized_keys` с сервера
- Во время `apt upgrade` пересобрался и перезапустился SSH-сервис, сессия оборвалась — переподключились без потери данных
- Правки `PasswordAuthentication no` / `PermitRootLogin no` в `/etc/ssh/sshd_config` не срабатывали — хостинг-провайдер уже положил `/etc/ssh/sshd_config.d/40-hosting.conf` с явными `yes` для обоих параметров. Файлы из `sshd_config.d/` подключаются в начале главного конфига, и в SSH побеждает первое встреченное значение — пришлось редактировать именно `40-hosting.conf`, а не основной файл. Обнаружили через `sudo sshd -T` — команда показывает реально применяемую конфигурацию, а не то, что написано в конкретном файле
- Смена `Port` в `sshd_config` не сработала сама по себе: в Ubuntu 24.04 SSH запускается через systemd socket activation (`ssh.socket`), у которого порт задан отдельно (`ListenStream=22`) и не читается из `sshd_config`. Пришлось переопределить сокет-юнит через `systemctl edit ssh.socket` (`ListenStream=` для сброса + `ListenStream=2244`), затем `daemon-reload` и перезапуск `ssh.socket`
